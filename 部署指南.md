# 🚀 GOLDEN DMS 部署指南

完整的部署教學，從本地測試到線上發布

---

## 目錄
1. [本地部署](#本地部署)
2. [GitHub Pages 部署](#github-pages-部署)
3. [其他雲端平台](#其他雲端平台)
4. [自架伺服器](#自架伺服器)
5. [故障排除](#故障排除)

---

## 本地部署

### 方法一：直接開啟（最簡單）

**適用情境**：個人使用、快速測試

**步驟**：
1. 下載 `index.html`
2. 雙擊檔案，使用預設瀏覽器開啟
3. 完成！

**優點**：
- ✅ 零設定
- ✅ 立即可用
- ✅ 無需網路（首次需要）

**缺點**：
- ⚠️ 檔案路徑會顯示在網址列
- ⚠️ 無法分享給他人

---

### 方法二：使用 Python 簡易伺服器

**適用情境**：開發測試、區域網路分享

**前置需求**：已安裝 Python 3

**步驟**：
```bash
# 1. 打開終端機/命令提示字元

# 2. 切換到檔案所在目錄
cd /path/to/golden-dms

# 3. 啟動伺服器（Python 3）
python -m http.server 8000

# 如果是 Python 2
python -m SimpleHTTPServer 8000

# 4. 在瀏覽器開啟
# http://localhost:8000
```

**區域網路分享**：
```bash
# 1. 查詢本機 IP
# Windows: ipconfig
# Mac/Linux: ifconfig

# 2. 其他裝置訪問
# http://你的IP:8000
# 例如：http://192.168.1.100:8000
```

**停止伺服器**：按 `Ctrl + C`

---

### 方法三：使用 Node.js

**前置需求**：已安裝 Node.js

**步驟**：
```bash
# 1. 安裝 http-server（全域安裝）
npm install -g http-server

# 2. 切換到檔案目錄
cd /path/to/golden-dms

# 3. 啟動伺服器
http-server -p 8000

# 4. 開啟瀏覽器
# http://localhost:8000
```

**進階選項**：
```bash
# 指定 IP 和 Port
http-server -a 192.168.1.100 -p 8080

# 啟用 CORS（跨域存取）
http-server --cors

# 啟用快取
http-server -c-1
```

---

## GitHub Pages 部署

### 完整步驟

**前置需求**：
- GitHub 帳號
- Git 已安裝（選用）

#### 方法 A：使用 GitHub 網頁介面（推薦）

**步驟**：
1. **登入 GitHub**
   - 前往 https://github.com
   - 登入你的帳號

2. **建立新倉庫**
   - 點擊右上角 `+` → `New repository`
   - Repository name: `golden-dms`（或任意名稱）
   - Description: `發文管理系統`
   - 選擇 `Public`
   - ✅ 勾選 `Add a README file`
   - 點擊 `Create repository`

3. **上傳檔案**
   - 在倉庫頁面點擊 `Add file` → `Upload files`
   - 將 `index.html` 拖曳到上傳區
   - Commit message: `Initial commit`
   - 點擊 `Commit changes`

4. **啟用 GitHub Pages**
   - 進入 `Settings`（倉庫設定）
   - 左側選單點擊 `Pages`
   - Source 選擇 `main` 分支
   - 點擊 `Save`
   - 等待 1-2 分鐘

5. **訪問網站**
   - 網址會顯示在 Pages 設定頁面
   - 格式：`https://你的帳號.github.io/golden-dms/`

#### 方法 B：使用 Git 命令列

**步驟**：
```bash
# 1. 建立本地倉庫
cd /path/to/golden-dms
git init

# 2. 新增檔案
git add index.html
git commit -m "Initial commit"

# 3. 連接遠端倉庫
git remote add origin https://github.com/你的帳號/golden-dms.git

# 4. 推送到 GitHub
git branch -M main
git push -u origin main

# 5. 啟用 GitHub Pages（使用網頁介面）
```

### 更新網站

**網頁介面**：
1. 進入倉庫
2. 點擊 `index.html`
3. 點擊鉛筆圖示（編輯）
4. 貼上新版本內容
5. Commit changes

**Git 命令列**：
```bash
# 1. 修改檔案後
git add index.html
git commit -m "Update to v1.1"
git push
```

### 自訂網域（進階）

**步驟**：
1. 購買網域（如 GoDaddy、Cloudflare）
2. 在 DNS 設定中新增 CNAME 記錄
   - Name: `www`
   - Value: `你的帳號.github.io`
3. 在 GitHub Pages 設定中填入自訂網域
4. 等待 DNS 生效（24-48 小時）

---

## 其他雲端平台

### Netlify（推薦）

**優點**：
- ✅ 免費 SSL 憑證
- ✅ 自動部署
- ✅ 自訂網域
- ✅ 表單處理

**步驟**：
1. 註冊 Netlify 帳號（https://netlify.com）
2. 點擊 `Add new site` → `Deploy manually`
3. 將 `index.html` 拖曳到上傳區
4. 完成！自動取得網址

**從 GitHub 部署**：
1. 連接 GitHub 帳號
2. 選擇倉庫
3. 自動部署

### Vercel

**步驟**：
1. 註冊 Vercel（https://vercel.com）
2. 點擊 `Add New Project`
3. 選擇 GitHub 倉庫
4. 點擊 `Deploy`

### Cloudflare Pages

**步驟**：
1. 登入 Cloudflare
2. 進入 `Pages`
3. 連接 GitHub
4. 選擇倉庫並部署

---

## 自架伺服器

### Nginx

**設定檔範例**：
```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /var/www/golden-dms;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # 啟用 gzip 壓縮
    gzip on;
    gzip_types text/html text/css application/javascript;
}
```

**部署步驟**：
```bash
# 1. 複製檔案到伺服器
scp index.html user@your-server:/var/www/golden-dms/

# 2. 重新載入 Nginx
sudo nginx -t
sudo systemctl reload nginx
```

### Apache

**.htaccess 範例**：
```apache
# 啟用壓縮
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/css application/javascript
</IfModule>

# 快取設定
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType text/html "access plus 1 hour"
    ExpiresByType text/css "access plus 1 week"
    ExpiresByType application/javascript "access plus 1 week"
</IfModule>
```

---

## 故障排除

### 問題 1：網頁顯示空白

**可能原因**：
- 字體或圖示 CDN 載入失敗

**解決方案**：
```bash
# 檢查網路連線
# 檢查瀏覽器控制台（F12）的錯誤訊息
# 確認 CDN 網址是否正常
```

### 問題 2：資料遺失

**可能原因**：
- 清除瀏覽器資料
- 使用無痕模式

**解決方案**：
- 定期匯出 CSV 備份
- 使用固定瀏覽器
- 不使用無痕模式

### 問題 3：手機版顯示異常

**可能原因**：
- 瀏覽器版本過舊

**解決方案**：
- 更新瀏覽器到最新版本
- 使用 Chrome、Safari、Firefox

### 問題 4：GitHub Pages 404 錯誤

**可能原因**：
- 檔案路徑錯誤
- Pages 尚未啟用

**解決方案**：
```bash
# 1. 確認檔案名稱為 index.html
# 2. 確認在 Settings > Pages 已啟用
# 3. 等待 1-2 分鐘讓系統建置
```

### 問題 5：CSV 匯出失敗

**可能原因**：
- 瀏覽器阻擋下載

**解決方案**：
- 檢查瀏覽器下載設定
- 允許網站下載檔案

---

## 效能優化

### 1. 啟用壓縮
```nginx
# Nginx
gzip on;
gzip_types text/html text/css application/javascript;
```

### 2. 瀏覽器快取
```html
<!-- 在 index.html 的 <head> 中新增 -->
<meta http-equiv="Cache-Control" content="max-age=86400">
```

### 3. CDN 加速
- 考慮使用 Cloudflare CDN
- 提升全球載入速度

---

## 安全性建議

### 1. HTTPS
- GitHub Pages 自動提供
- Netlify/Vercel 自動提供
- 自架伺服器需設定 SSL 憑證

### 2. 定期備份
```bash
# 設定每日自動匯出（可用 cron job）
# 將 CSV 備份到雲端硬碟
```

### 3. 存取控制
- 如需限制存取，考慮使用：
  - HTTP Basic Auth
  - OAuth 登入
  - IP 白名單

---

## 監控與維護

### 1. Google Analytics（選用）
```html
<!-- 在 </head> 前加入 -->
<script async src="https://www.googletagmanager.com/gtag/js?id=GA_MEASUREMENT_ID"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'GA_MEASUREMENT_ID');
</script>
```

### 2. 錯誤追蹤
- 使用 Sentry 或 LogRocket
- 監控 JavaScript 錯誤

### 3. 定期更新
- 檢查 CDN 資源是否正常
- 瀏覽器相容性測試

---

## 總結

**推薦部署方案**：

| 使用情境 | 推薦方案 | 難度 |
|---------|---------|------|
| 個人使用 | 本地開啟 | ⭐ |
| 團隊測試 | Python 伺服器 | ⭐⭐ |
| 正式發布 | GitHub Pages | ⭐⭐⭐ |
| 商業用途 | Netlify/Vercel | ⭐⭐⭐ |
| 企業內部 | 自架伺服器 | ⭐⭐⭐⭐⭐ |

---

需要協助？歡迎聯繫：
- 📧 Email：[您的聯絡信箱]

**GOLDEN DMS** - 部署簡單，使用方便 🚀
